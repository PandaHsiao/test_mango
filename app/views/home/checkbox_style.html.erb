<% self.formats = [:json, :html] %>

<label><%= @name %></label>
</br>
<% @data.each_with_index do |x,i| %>
    <input type="checkbox" name="checkbox_group_<%= @name %>" value="<%= i %>"> <%= x %>
<% end %>
</br></br>

